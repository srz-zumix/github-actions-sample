name: Setup-xxx
on:
  workflow_dispatch:
  pull_request:

jobs:
  dotnet:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 7.x
    - run: dotnet --list-sdks
    - uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.x
    - run: dotnet --list-sdks
