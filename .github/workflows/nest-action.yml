name: Nest action
on:
  pull_request:

permissions:
  contents: read

env:
  GH_TOKEN: ${{ github.token }}

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/nest1
        with:
          test: "nest test"
